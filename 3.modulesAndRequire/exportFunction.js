const counterFunction = function(arr) {
    return arr.length
}
const adder = function(a, b) {
    return a + b
}
const pi = 3.14

module.exports = {
    counter:counterFunction,
    adder,
    pi
}

